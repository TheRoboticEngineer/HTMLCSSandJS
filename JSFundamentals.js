// // Create the following variables;
// name, which is a string set to your current name
// dayOfBirth, which is a number set to the day of your birth month
// See what happens when you have multiple variables of the same name. Which one takes precedence?
// Write some JavaScript that prompt’s the user for their favorite color. Once the user has submitted a favorite color, log that color to the console along with a friendly message.
// Create a string that contains both single quotes and double quotes.
// What is the difference between null and undefined?
// What is NaN in JavaScript? What is the typeof NaN?
// You can declare a variable by typing let thing;. What is the value of thing?
// 

let name = SamairaStewart
let DateOfBirth = 26
typeOf DateOfBirth

// When you have multiple variables of the same name, the one that was created most recently takes precendece, however if you use two let name functions, you will get an error.

let UserInput = prompt("what is your favorite color?")
console.log("Your favorite color is" + UserInput + "That's a great color!")

let MomsQuote = 'Mom told me "sometimes in life, you need to take risks"'

// Null is the intentional absence of a value (usually decided by the programmer), but undefined is when a variable does not have a defined value
// NaN is a Java represensation for a value that is not a number. NaN is seen as a number in JS.
// If we type let thing, thing is undefined because we have not declared a value or string
